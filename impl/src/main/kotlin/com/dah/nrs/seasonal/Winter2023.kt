package com.dah.nrs.seasonal

import com.dah.nrs.dsl.*
import com.dah.nrs.exts.*
import kotlin.time.Duration.Companion.hours
import kotlin.time.Duration.Companion.minutes
import kotlin.time.Duration.Companion.seconds

fun DSLScope.Winter2023() {
    // himeno sena and her consequences :skull:
    // tl;dr: cultural doomerism died, cultural revisionism and
    // cultural believism grew, observism has its own party (the COP),
    // remodernization, the return of romance and the weakening of
    // the heisenberg uncertainty principle

    Entry {
        id = "A-MAL-52736"

        // pogpega it's le cat !!1!1!!
        bestGirl = "Lainie Cyan"
        // imagine still talking about le cat like it was in arc 2
        // like bruh himeno sena exists now you fucking dumbass
        bestGirl = "Anisphia Wynn Palletia"

        AnimeConsumedProgress(EntryStatus.Watching, 1.0, 1)
        Visual(VisualKind.Animated, 0.5, 0.3)

        // the first ep already gave me chill :skull:
        AEI(0.2, Emotion.CP to 0.8, Emotion.MP to 0.2)
    }

    Entry {
        id = "A-MAL-49980"

        // lmfao "escha 2023"
        bestGirl = "Ann Halford"

        AnimeConsumedProgress(EntryStatus.Watching, 1.0, 1)
        Visual(VisualKind.Animated, 0.4, 0.6)
    }

    Entry {
        id = "A-MAL-49612"

        // lmfao "wilbell 2023"
        // jk lol
        bestGirl = "Tiana"

        AnimeConsumedProgress(EntryStatus.Watching, 1.0, 1)
        Visual(VisualKind.Animated, 0.3, 0.6)

        // focus on [trust], somewhat interesting
        // idk will this be able to continued to other eps tho
        NEI(0.2, Emotion.AP)
    }

    // ngl this is way better than previous season
    // it's somewhat esoteric too, like it's full fantasy
    // the only "idol" factor is just a "joke" in the exiled
    // from party anime

    // the reason of all this is that fucking koikake girl
    // i fucking hate her
}
